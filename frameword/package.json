{
  "name": "frameword",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "bun test packages/panels-core packages/stax-migrate",
    "build": "cd apps/crm-specimen && bunx vite build",
    "dev": "cd apps/crm-specimen && bunx vite --port 5799",
    "build:packages": "cd packages/panels-core && bun run build && cd ../panels-react && bun run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
