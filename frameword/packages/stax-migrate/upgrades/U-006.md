# U-006 — First-run auto-open · ⌘K live stores · mobile dashboard switcher

TARGET: {{TARGET}}
REFERENCE: frameword/apps/crm-specimen/src/App.tsx (first-run effect, Palette items,
.sb-dash) · styles.css

## DETECT
- A truly fresh visit (no snapshot, no deep link) renders an empty stage.
- The command palette only lists static/doc content — live entities (rows, notes,
  records) are unfindable.
- Below the topbar-nav breakpoint, other dashboards are unreachable.

## APPLY
1. First run: no storage key + no URL hash → auto-open the landing space (the mechanic
   demos itself). Never auto-open when the user deliberately closed everything.
2. Palette: index the LIVE stores (every entity a user can name) — each hit deep-links
   through the store resolvers; subscribe the palette to those stores.
3. Compact sidebar: a dashboard switcher row (one icon+label segment per dashboard) at
   the top of the overlay.

## VERIFY
- localStorage.clear() + reload → a real space is open.
- Palette search finds a live record by name and opens its full chain.
- 500px viewport: every dashboard reachable in ≤ 2 taps.
