# U-008 — Bundle diet: lazy heavy panel modules; trim icon mega-packs

TARGET: {{TARGET}}
REFERENCE: frameword/apps/crm-specimen — boardStore.ts / RichNotes.tsx seams, Flags.tsx,
vite build output

## DETECT
- One JS chunk carrying whiteboard/editor libraries used by 1-2 panel types.
- An icon/flag/font pack imported wholesale for a handful of glyphs.

## APPLY
1. Split each heavy panel module along a STORE-ONLY seam: the store + types +
   cross-module helpers in a light module the shell imports statically; the heavy
   renderer behind React.lazy + Suspense at the panelType switch.
2. Replace pack imports by the actually-used subset as inline SVGs/components.

## VERIFY
- Build output: the heavy library sits in an async chunk; it loads only when the panel
  type first opens (network/resource proof).
- Before/after sizes of the entry JS and CSS, pasted.
