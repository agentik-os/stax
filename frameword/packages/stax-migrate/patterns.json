{
  "generated": "from DESIGN-SPEC.md console/analytics table",
  "patterns": [
    {
      "legacy": "API keys / tokens page (create, reveal once, roll, revoke)",
      "panelType": "pfkeys",
      "grammar": "`pfkeys`: a `.dt` of keys with masked secrets, a REVEAL-ONCE dialog on creation (the secret is never re-shown), roll and revoke as row actions, the row drills to the key's panel",
      "reference": "/#/console/keys"
    },
    {
      "legacy": "Team / members / seats page with invitations",
      "panelType": "pfpeople",
      "grammar": "`pfpeople`: ONE segmented collection (members + pending invitations), the invite composer in the FOOT, role as segments per row, owner-guarded destructive actions",
      "reference": "/#/console/people"
    },
    {
      "legacy": "Projects / environments / workspaces switcher page",
      "panelType": "pfprojects",
      "grammar": "`pfprojects`: rows carrying geography, retention and spend; create in the foot, archive from the detail",
      "reference": "/#/console/projects"
    },
    {
      "legacy": "Billing page (plan, payment method, invoices)",
      "panelType": "pfbilling",
      "grammar": "`pfbilling`: credit balance + auto-recharge in the body, sub-pages as DRILLS (payment methods, billing history) \u2014 the hub pattern, never tabs",
      "reference": "/#/console/billing"
    },
    {
      "legacy": "Usage / metering / consumption charts",
      "panelType": "pfusage",
      "grammar": "`pfusage`: a range switcher as foot segments over one dataset, mono tabular figures, the chart is a block not a page",
      "reference": "/#/console/usage"
    },
    {
      "legacy": "Rate limits / quotas / spend caps",
      "panelType": "pflimits",
      "grammar": "`pflimits`: threshold rows with inline edit, the alert address as a field, never a modal",
      "reference": "/#/console/limits"
    },
    {
      "legacy": "Live log stream / event console",
      "panelType": "pflogs",
      "grammar": "`pflogs`: append-only mono scrollback with level colours, a line drills to its error context",
      "reference": "/#/console/logs"
    },
    {
      "legacy": "Service status / health page",
      "panelType": "pfhealth",
      "grammar": "`pfhealth`: component rows with state dots (accent ramp, never green), incident history as drills",
      "reference": "/#/console/health"
    },
    {
      "legacy": "Security / audit / access review",
      "panelType": "pfsecurity",
      "grammar": "`pfsecurity`: posture rows with a state per control; the evidence drills out of the row",
      "reference": "/#/console/security"
    },
    {
      "legacy": "Data controls / retention / privacy toggles",
      "panelType": "pfcontrols",
      "grammar": "`pfcontrols`: switch rows with their consequence written beside them, saved on flip",
      "reference": "/#/console/controls"
    },
    {
      "legacy": "Asset / media library",
      "panelType": "pfimages",
      "grammar": "`pfimages`: a density-switchable gallery, the item drills to its detail panel",
      "reference": "/#/studio/images"
    },
    {
      "legacy": "API playground / request builder",
      "panelType": "pfprompt",
      "grammar": "`pfprompt`: the config column becomes stacked sections, model and options are SEGMENTS (never native selects), the composer is anchored in the FOOT",
      "reference": "/#/studio/prompt"
    },
    {
      "legacy": "Embedded terminal / CLI",
      "panelType": "pfterm",
      "grammar": "`pfterm`: mono scrollback in the body, the PROMPT in the foot, ArrowUp history, verbs drive the workspace through the bridge",
      "reference": "/#/studio/terminal"
    },
    {
      "legacy": "Support / assistant chat",
      "panelType": "pfchat",
      "grammar": "`pfchat`: the thread IS the panel body, the composer is the foot, slash commands speak the bridge",
      "reference": "/#/studio/chat"
    },
    {
      "legacy": "Realtime / streaming session surface",
      "panelType": "pfrealtime",
      "grammar": "`pfrealtime`: the live session as a panel, its controls in the foot",
      "reference": "/#/studio/realtime"
    },
    {
      "legacy": "Getting-started hub / integration launcher",
      "panelType": "pfhub",
      "grammar": "`pfhub`: paste-ready recipes as rows, each drilling into the prefilled composer",
      "reference": "/#/studio/hub"
    },
    {
      "legacy": "Conversion funnel report",
      "panelType": "ancfo",
      "grammar": "`ancfo`: the WALK shape. A funnel or a forecast is a computation descending to a result, so it renders as the P&L walk does: one row per term, sub-terms indented, totals ruled, the plan outlined and the actual filled, and the delta coloured by MEANING (a cost under plan is favourable). See `stax-migrate shapes walk`.",
      "reference": "/#/analytics/cfo"
    },
    {
      "legacy": "Traffic sources / attribution report",
      "panelType": "pfusage",
      "grammar": "`pfusage`: the METERED shape. Attribution is one quantity split by dimension, so it renders as usage does: a range segmented control, an inline svg bar series, then a by-source breakdown as proportional rows. See `stax-migrate shapes metered`.",
      "reference": "/#/console/usage"
    },
    {
      "legacy": "Revenue by segment / MRR breakdown",
      "panelType": "antreasury",
      "grammar": "`antreasury`: the LADDER shape. Revenue by segment is entities compared by magnitude, so it renders as the cash ladder does: one row per segment, a state dot, a bar sized against the set's maximum, the figure right-aligned tabular. See `stax-migrate shapes ladder`.",
      "reference": "/#/analytics/treasury"
    },
    {
      "legacy": "Forecast / projection report",
      "panelType": "ancfo",
      "grammar": "`ancfo`: the WALK shape. A funnel or a forecast is a computation descending to a result, so it renders as the P&L walk does: one row per term, sub-terms indented, totals ruled, the plan outlined and the actual filled, and the delta coloured by MEANING (a cost under plan is favourable). See `stax-migrate shapes walk`.",
      "reference": "/#/analytics/cfo"
    }
  ]
}